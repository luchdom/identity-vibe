# Development Environment Configuration
# Used for local development with docker-compose -f docker-compose.yml -f docker-compose.dev.yml

# Database
POSTGRES_PASSWORD=postgres

# External URLs (accessible from browser/external clients)
EXTERNAL_AUTH_URL=https://localhost:5000
EXTERNAL_GATEWAY_URL=http://localhost:5002
EXTERNAL_CLIENT_URL=http://localhost:5173

# Internal URLs are handled by docker-compose.yml
# INTERNAL_AUTH_URL=http://authserver:8080
# INTERNAL_GATEWAY_URL=http://gateway:8080
# INTERNAL_ORDERS_URL=http://orders:8080