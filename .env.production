# Production Environment Configuration
# Used for production deployment with docker-compose -f docker-compose.yml -f docker-compose.prod.yml

# Database
POSTGRES_PASSWORD=your-secure-production-postgres-password

# External URLs (accessible from browser/external clients)
# Replace with your actual production domain
EXTERNAL_AUTH_URL=https://auth.yourdomain.com
EXTERNAL_GATEWAY_URL=https://api.yourdomain.com
EXTERNAL_CLIENT_URL=https://app.yourdomain.com

# Internal URLs are handled by docker-compose.prod.yml
# INTERNAL_AUTH_URL=http://authserver:80
# INTERNAL_GATEWAY_URL=http://gateway:80
# INTERNAL_ORDERS_URL=http://orders:80

# SSL/TLS Configuration (if using custom certificates)
# ASPNETCORE_Kestrel__Certificates__Default__Path=/path/to/cert.pfx
# ASPNETCORE_Kestrel__Certificates__Default__Password=your-cert-password