# Staging Environment Configuration
# Used for staging deployment with docker-compose -f docker-compose.yml -f docker-compose.prod.yml

# Database
POSTGRES_PASSWORD=your-staging-postgres-password

# External URLs (accessible from browser/external clients)
# Replace with your actual staging domain
EXTERNAL_AUTH_URL=https://auth-staging.yourdomain.com
EXTERNAL_GATEWAY_URL=https://api-staging.yourdomain.com
EXTERNAL_CLIENT_URL=https://app-staging.yourdomain.com

# Internal URLs are handled by docker-compose.prod.yml
# INTERNAL_AUTH_URL=http://authserver:80
# INTERNAL_GATEWAY_URL=http://gateway:80
# INTERNAL_ORDERS_URL=http://orders:80